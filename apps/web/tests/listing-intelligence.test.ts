import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";import {formatScore} from "../app/listing-intelligence/listing-intelligence-client";
describe("S5 listing intelligence",()=>{it("is routable from navigation",()=>{const app=join(process.cwd(),"app");expect(readFileSync(join(app,"listing-intelligence","page.tsx"),"utf8")).toContain("ListingIntelligenceClient");expect(readFileSync(join(app,"layout.tsx"),"utf8")).toContain('["Listing Intelligence", "/listing-intelligence"]')});it("formats explicit completeness values",()=>{expect(formatScore(null)).toBe("—");expect(formatScore(85)).toBe("85/100")})});
